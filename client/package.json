{
  "private": true,
  "scripts": {
    "start": "npm --prefix server start",
    "dev:client": "npm --prefix client run dev",
    "dev:server": "npm --prefix server start"
  },
  "devDependencies": {
    "cross-env": "^10.0.0"
  }
}
